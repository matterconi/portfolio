/** @type {import('next').NextConfig} */
const nextConfig = {
    // Add your custom Next.js configurations here if needed
  };
  
  // Export the configuration without Sentry integration
  export default nextConfig;
  